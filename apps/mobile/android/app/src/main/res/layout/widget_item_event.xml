<?xml version="1.0" encoding="utf-8"?>
<!--
  Calendar-event row inside the widget ListView.
  Layout:
    [ 4dp colour bar ] [ title (14sp) ]
                       [ time  (11sp) ]
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_event_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="3dp"
    android:paddingBottom="3dp"
    android:paddingEnd="4dp">

    <!-- Coloured left accent bar (colour set programmatically) -->
    <View
        android:id="@+id/item_event_color_bar"
        android:layout_width="4dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="6dp"
        android:minHeight="32dp"
        android:background="#4A90E2" />

    <!-- Text block: title + time -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/item_event_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/widget_text_primary"
            android:text="Event title" />

        <TextView
            android:id="@+id/item_event_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/widget_text_secondary"
            android:text="09:00 – 10:00" />
    </LinearLayout>
</LinearLayout>
